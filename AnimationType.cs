using System;

namespace DistinctionTask{
    /// <summary>
    /// Public enumeration for different animation types used in the game.
    /// </summary>
    public enum AnimationType
    {
        Idle,
        Walk,
        StatueSkill,
        Attack,
        AttackSkill,
        AttackStatueSkill,
        Death
    }
}