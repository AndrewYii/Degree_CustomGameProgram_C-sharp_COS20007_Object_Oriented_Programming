using System;

namespace DistinctionTask{
    /// <summary>
    /// Public enumeration to define the difficulty levels of the game.
    /// </summary>
    public enum Difficulty
    {
        Easy,
        Medium,
        Hard
    }
}