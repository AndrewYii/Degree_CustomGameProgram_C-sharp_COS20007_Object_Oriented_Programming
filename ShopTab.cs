using System;

namespace  DistinctionTask{
    /// <summary>
    /// Public enumeration to determine the type of shop tab.
    /// </summary>
    public enum ShopTab
    {
        Buy,
        Sell
    }
}