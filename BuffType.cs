using System;

namespace DistinctionTask{
    /// <summary>
    /// Public enumeration to represent the type of buff.
    /// </summary>
    public enum BuffType
    {
        Positive,
        Negative
    }
    
}