using System;

namespace DistinctionTask{
    /// <summary>
    /// This public enumeration defines the different types of weapons available in the game.
    /// </summary>
    public enum WeaponType
    {
        Sword,
        Bow,
        Axe
    }
}