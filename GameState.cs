using System;

namespace DistinctionTask{
    /// <summary>
    /// Public enumeration to determine the different game states.
    /// </summary>
    public enum GameState
    {
        MainMenu,
        StoryIntro,
        CharacterSelection,
        NameInput,
        Exploring,
        InBattle,
        Inventory,
        GameOver,
        Credit
    }
}