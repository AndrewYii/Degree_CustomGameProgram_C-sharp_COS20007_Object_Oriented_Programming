using System;

namespace DistinctionTask {
    /// <summary>
    /// Publiec enumeration to determine the type of potion.
    /// </summary>
    public enum PotionType
    {
        Healing,
        ExpBoost,
        ReduceCooldown,
        Mana
    }
}
